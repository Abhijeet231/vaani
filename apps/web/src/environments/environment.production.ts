export const environment = {
  apiBaseUrl: 'https://vaani-api-gx49.onrender.com',
  // Pre-launch: the live site shows only the waitlist page. Flip to false and
  // rebuild to bring the full app back at launch.
  waitlistOnly: true,
};
