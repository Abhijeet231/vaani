export const environment = {
  apiBaseUrl: 'https://vaani-api-gx49.onrender.com',
  // Launched 2026-09-11. Still on Razorpay test keys at this point — recharge
  // packs are free to "buy" with public test card numbers, so free-trial and
  // recharge abuse both draw down real Sarvam API spend until live keys are in.
  waitlistOnly: false,
};
