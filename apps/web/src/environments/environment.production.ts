// TODO: replace with the real Render API URL once that service is created
// (see PROGRESS.md's deployment entry — this is a placeholder, not a live URL).
export const environment = {
  apiBaseUrl: 'https://TODO_RENDER_API_URL',
};
